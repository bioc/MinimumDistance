useDynLib("MinimumDistance", .registration=TRUE)
importFrom(utils, setTxtProgressBar, txtProgressBar)#, stack)
importFrom(graphics, abline, axis, layout, legend, mtext, par, plot,
	   polygon, rect, segments, text)
importFrom(lattice, xyplot, simpleKey, panel.grid, panel.xyplot, lrect, ltext,
	   lpoints, panel.number)
importClassesFrom(Biobase, eSet)
importMethodsFrom(Biobase, annotatedDataFrameFrom, updateObject, "[", "$")
importFrom(Biobase, assayDataElementReplace, sampleNames, varLabels, rowMedians, "sampleNames<-")
import(SNPchip)
##importFrom(SNPchip, integer2chromosome)
importFrom(stats, mad)
importClassesFrom(IRanges, RangedData, IRanges, IntervalTree,
		  DataTable, Vector, Annotated,
		  List, SimpleList, CompressedList,
		  RangesList, SimpleRangesList,
		  RangedDataList,
		  DataFrame,
		  DataTableORNULL)
importClassesFrom(oligoClasses, RangedDataCNV, RangedDataCBS, RangedDataHMM)
importMethodsFrom(IRanges, start, end, width,"[", "[[","[<-", "[[<-", "$",
		  nrow, "start<-", "end<-", rbind, stack)
importMethodsFrom(oligoClasses, state, coverage2, chromosome)
importFrom(oligoClasses, baf, lrr, RangedDataCNV, RangedDataCBS, RangedDataHMM)
importFrom(IRanges, IRanges, RangedData, RangedDataList,
	   findOverlaps, countOverlaps,
	   ranges, values,
	   matchMatrix, queryHits, subjectHits)
importFrom(msm, qtnorm, dtnorm)
##importFrom(ff, filename)
importFrom(DNAcopy, CNA, segment, smooth.CNA, getbdry)
importMethodsFrom(VanillaICE, xyplot, xyplot2)
exportClasses(DataFrameCNV, ##RangedDataCNV, RangedDataCBS, RangedDataHMM,
##	      LogRratioSet,
	      TrioSet, TrioSetList,
	      Pedigree, SampleSheet)
exportMethods("[", allNames,
	      baf, "baf<-",
	      calculateMindist,
	      computeBayesFactor,
	      fatherNames, "fatherNames<-",
	      ##logR, "logR<-",
	      lrr, "lrr<-",
##	      mad, "mad<-",
	      ##mad.sample,
	      "mad.sample<-",
	      ##mad.marker,
	      "mad.marker<-",
	      mindist,
	      "mindist<-",
##	      mad.mindist,
	      "mad.mindist<-",
	      minimumDistance,
	      motherNames, "motherNames<-",
	      ncol,
	      nrow,
	      offspringNames, "offspringNames<-",
	      pedigree,
	      prune,
	      rbind,
	      sampleSheet,
	      stack,
	      ##trioNames,
	      trios,
	      updateObject,
	      width,
	      xsegment,
	      xyplot,
	      initialize)
export(concAtTop,
       calculateMADlrr,
       ##constructTrioSetList,
       TrioSetList,
       correspondingCall,
       discAtTop,
       fmoNames,
       gridlayout,
       isDenovo,
##       minimumDistance,
##       minimumDistanceCalls,
       minimumDistancePlot,
       narrow, SampleSheet, xyplot, xypanel)
##       phenoDataArray)


